server:
  port: 3000

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    com.auth0.example: TRACE

okta:
  oauth2:
    issuer: https://{DOMAIN}/
    client-id: {CLIENT_ID}
    client-secret: {CLIENT_SECRET}
